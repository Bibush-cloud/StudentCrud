micronaut:
  application:
    name: studentcrud
netty:
  default:
    allocator:
      max-order: 3


datasources:
  default:
    url: jdbc:postgresql://localhost:5432/marksheet
    username: postgres
    password: bibush123
    dialect: POSTGRES

jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        show_sql: true
